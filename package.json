{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "homepage": "https://github.com/Yuvarajkumar-S/seyal",

  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}